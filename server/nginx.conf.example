# =============================================
# Exemplo de configuração Nginx para o sistema
# =============================================
# Opção A: Nginx como proxy reverso para o Node.js
# (recomendado - o Node serve tanto a API quanto o frontend)

server {
    listen 80;
    server_name seudominio.com.br;

    # Redirecionar HTTP para HTTPS (opcional, mas recomendado)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
    }
}

# =============================================
# Opção B: Nginx serve o frontend estático
# e faz proxy apenas da API para o Node.js
# =============================================

# server {
#     listen 80;
#     server_name seudominio.com.br;
#
#     root /var/www/consulta-ncm/dist;
#     index index.html;
#
#     # API - proxy para Node.js
#     location /api/ {
#         proxy_pass http://127.0.0.1:3001;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
#
#     # Frontend - SPA fallback
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     # Cache de assets estáticos
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
# }
